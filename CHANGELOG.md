# 记账本 MoneyBook 变更日志

版本号: `主版本.次版本.补丁` — versionName 显示给用户, versionCode(整数递增) 用于应用商店判定更新。
每次发布: 更新本文件 → bump `android/gradle.properties` 版本 → `npm run build`(自动同步 version.js) → 打包 APK → `git tag vX.Y.Z` 并推送。

---

## v3.0.3 (2026-09-13) — 精简功能
**移除**
- 删除"自定义报表"功能(ReportBuilder 页/入口/路由/样式)
- 删除"账户管理"功能(Accounts 页/入口/路由/首页与统计的净资产入口/记账弹窗的账户字段)
- 保留: 账号/预算/拆分/撤销/批量等核心能力与底层数据兼容

## v3.0.2 (2026-09-13) — Bug 修复 (Web 降级层与真机一致性)
**修复**
- 内存降级层 `dailySum` 字段与 Java 对齐(`day`), 并为同日收支按 `day+type` 分组(避免合并覆盖)
- `listTransactions` 内存实现补齐 `type/分类/关键词` 筛选(含拆分子分类命中)
- Web 分支 `sumByCategory/dailySum` 丢失 `type` 过滤的问题(db 层统一"取数+公共过滤")

## v3.0.1 (2026-09-13) — Bug 修复 + Web 可用性
**新增**
- Web/预览环境内存降级数据层(`src/db/memory.js`): 无原生桥时账户/预算/交易/统计全功能可用,便于浏览器调试演示
**修复**
- 修复编辑账户时把"当前余额"误写入期初,导致余额双重计入的问题(编辑仅改名称/类型/图标,期初保留)
**版本管理**
- 建立 CHANGELOG + git tag 发布流程

## v3.0.0 (2026-09-13) — 借鉴 Actual Budget 全面升级
- **账户体系**: 多账户管理(现金/银行卡/支付宝/微信…)、净资产总览、账户间转账联动; 自动记账按商户绑定账户
- **信封预算**: 底部新增"预算"页签; 月度手动分配额度到分类; 分类完成度进度条(富余/将超/超支三色); 未花余额结转下月
- **交易增强**: 拆分交易(一笔分多分类)、撤销/重做(会话级)、账单批量多选改分类/删除、商户→分类记忆自动带出
- **报表**: 净资产卡、现金流 12 月趋势、年累计同比、自定义报表(筛选组合 + 保存视图)
- **首页仪表盘**: 已花 Hero + 收入/净资产/预算进度/分类 TOP3 卡片网格
- **UI**: 紫渐变升级版视觉、亮暗双主题重写、字大疏朗、底部 5 页签导航
- **数据层**: accounts/budgets/tx_splits/商户映射表; queryTransactions 批量加载拆分明细(修复 N+1)

## 历史 (v2.x, 精简)
- v2.2.8x: 短信/通知自动记账、五层去重、智能分析(洞察/月对比/热力图)、桌面小组件、后台保活(四重)、手动记账恢复
- v2.1.x: 保活加固、监听源设置、诊断面板
- v1.x: 基础记账 + 短信识别 + 通知监听 + 小组件雏形

(注: 会员功能于 v3.0.0 前移除, 退回完全免费)